## Comandos Cypress básicos

- `npm install`  
  Instala as dependências do projeto.

- `npx cypress open`  
  Abre o Cypress para rodar os testes via interface gráfica.

- `npx cypress run`  
  Roda todos os testes no terminal (modo headless).